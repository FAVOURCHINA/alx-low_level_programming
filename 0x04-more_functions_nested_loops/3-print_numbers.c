#include "main.h"

/**
 * print_numbers - checks for checks for a digit (0 thriugh 9)
 *
 * Return: Always 0.
 */
void print_numbers(void)
{
	int C;

	for (C = 48; C < 58; C++)
	{
	_putchar(C);
	}
	_putchar('\n');
}
